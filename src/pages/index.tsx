import { useState, useEffect } from "react";
import type { NextPage } from "next";
import Head from "next/head";
import Header from "../components/Header";
import Sidebar from "../components/Sidebar";
import { trpc } from "../utils/trpc";
var FileSaver = require("file-saver");

const Home: NextPage = () => {
  
  // get data from api
  const downloadData = async () => {
    const data = await fetch("/api/download", {
      method: "POST",
      body: JSON.stringify({
        title: "foo",
        body: "bar",
        userId: 1,
      }),
      headers: {
        "Content-Type": "application/json",
        "Response-type": "blob",
      },
    }).then((res) => {
      return res.blob();
    });
    FileSaver.saveAs(data, "data.xlsx");
  };

  return (
    <>
      <Head>
        <title>The Gowdie</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      {/* <Header /> */}
      <Sidebar />
      {/* <main className="container mx-auto my-auto flex min-h-screen flex-col items-center justify-center p-4">
        <button onClick={() => downloadData()}>Test</button>
      </main> */}
    </>
  );
};

export default Home;
